#ifndef RAWSCORES_HPP
#define RAWSCORES_HPP

void rawscores(int& numscores, double* totalscores, int*ids, int& numproj, int* points, int* weight, int& numstudents, int*id, std::string* name);

#endif
